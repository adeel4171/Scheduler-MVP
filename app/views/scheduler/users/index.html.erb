<div class="container">
  <p id="notice"><%= notice %></p>

  <h1>Users</h1>
  <div class="row">
    <div class="col-md-4">
      <%= link_to 'New User', new_scheduler_user_path, class: 'btn btn-success' %>
    </div>
    <div class="col-md-4">
      <%= link_to('Logout', destroy_user_session_path, method: :delete, class: 'btn btn-danger') %> 
    </div>
  </div>




  <table class="table">
    <thead>
      <tr>
        <th> Name </th>
        <th> Gender </th>
        <th> Email </th>
      </tr>
    </thead>
    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td><%= user.name %></td>
          <td><%= user.gender %></td>
          <td><%= user.email %></td>
          <% if user.roles.first.name != "admin" %>
            <td><%= link_to 'View Schedules', scheduler_schedules_path(user_id: user.id) %>
          <% end %>
          <!--td><%#= link_to 'Show', scheduler_user_path(user) %></td-->
          <!--td><%#= link_to 'Edit', edit_scheduler_user_path(user) %></td-->
          <!--td><%#= link_to 'Destroy', scheduler_user_path(user), method: :delete, data: { confirm: 'Are you sure?' } %></td-->
        </tr>
      <% end %>
    </tbody>



  </table>

  

  <!-- <table>
    <thead>
      <tr>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <%# @users.each do |user| %>
        <tr>
          <td><%#= user.name %></td>
          <td><%#= user.gender %></td>
          <td><%#= user.email %></td>
          <td><%#= link_to 'View Schedules', scheduler_schedules_path(user_id: user.id) %>
          <td><%#= link_to 'Show', scheduler_user_path(user) %></td>
          <td><%#= link_to 'Edit', edit_scheduler_user_path(user) %></td>
          <td><%#= link_to 'Destroy', scheduler_user_path(user), method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <%# end %>
    </tbody>
  </table>
 -->
  <br>

  
</div>
